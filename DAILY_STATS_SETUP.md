# 📅 Ежедневная статистика SolardBot

## 🕛 **Время отправки: 00:00 МСК каждый день**

### 📋 **Как работает:**

1. **Автоматическая отправка** статистики каждый день в полночь по московскому времени
2. **Отправляется в админский чат** (ID: -4742593122)
3. **Статистика за вчерашний день** (не за сегодняшний)
4. **Работает независимо** от основного бота

### 🚀 **Запуск планировщика:**

#### **На Windows:**
```bash
python daily_stats_scheduler.py
```

#### **На Ubuntu:**
```bash
python3 daily_stats_scheduler.py
```

### ⚙️ **Настройка на Ubuntu сервере:**

#### **1. Создать systemd сервис:**
```bash
sudo nano /etc/systemd/system/solardbot-scheduler.service
```

#### **2. Содержимое сервиса:**
```ini
[Unit]
Description=SolardBot Daily Statistics Scheduler
After=network.target

[Service]
Type=simple
User=solardbot
Group=solardbot
WorkingDirectory=/home/solardbot/SolardBot
Environment=PATH=/home/solardbot/SolardBot/venv/bin
ExecStart=/home/solardbot/SolardBot/venv/bin/python daily_stats_scheduler.py
Restart=always
RestartSec=10

# Логирование
StandardOutput=append:/var/log/solardbot-scheduler/scheduler.log
StandardError=append:/var/log/solardbot-scheduler/scheduler-error.log
SyslogIdentifier=solardbot-scheduler

[Install]
WantedBy=multi-user.target
```

#### **3. Активировать сервис:**
```bash
sudo systemctl daemon-reload
sudo systemctl enable solardbot-scheduler.service
sudo systemctl start solardbot-scheduler.service
```

### 📊 **Что отправляется:**

- 👥 **Общее количество пользователей**
- 🆕 **Новых пользователей за день**
- 📊 **Общее количество действий**
- 🔧 **Популярные номера устройств**
- ❓ **Популярные вопросы**
- ⭐ **Топ пользователи**

### 🔍 **Проверка работы:**

#### **Статус сервиса:**
```bash
sudo systemctl status solardbot-scheduler.service
```

#### **Логи:**
```bash
sudo journalctl -u solardbot-scheduler.service -f
```

#### **Файловые логи:**
```bash
tail -f /var/log/solardbot-scheduler/scheduler.log
```

### ⚠️ **Важные моменты:**

1. **Время МСК** - скрипт автоматически определяет московское время
2. **Независимая работа** - планировщик работает отдельно от основного бота
3. **Автоперезапуск** - при сбое сервис автоматически перезапускается
4. **Логирование** - все действия записываются в логи

### 🆘 **Решение проблем:**

#### **Если статистика не отправляется:**
1. Проверьте статус сервиса: `sudo systemctl status solardbot-scheduler.service`
2. Проверьте логи: `sudo journalctl -u solardbot-scheduler.service`
3. Убедитесь, что токен настроен в `token.env`
4. Проверьте права доступа к файлам

#### **Если время неправильное:**
1. Убедитесь, что установлен `pytz`: `pip install pytz`
2. Проверьте часовой пояс сервера: `timedatectl`
3. Перезапустите сервис: `sudo systemctl restart solardbot-scheduler.service`

### 📝 **Пример лога:**
```
2025-10-08 00:00:01 - INFO - Наступила полночь в МСК, отправляем статистику...
2025-10-08 00:00:01 - INFO - Начинаем отправку ежедневной статистики... Время МСК: 2025-10-08 00:00:01
2025-10-08 00:00:02 - INFO - Ежедневная статистика отправлена в чат за 2025-10-07
```
